import { BookClient } from "./book-client"

export const metadata = {
  title: "Book Your Assessment Call",
  description: "Book a 15-20 minute assessment call with the UnSwap team.",
}

export default function BookPage() {
  return <BookClient />
}

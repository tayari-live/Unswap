-- AI-drafted, per-exchange agreement clauses, generated once and reused so the
-- downloaded agreement stays identical across downloads.
ALTER TABLE "SwapRequest" ADD COLUMN "agreementClauses" TEXT;
